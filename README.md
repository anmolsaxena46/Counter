# Counter
React Counter App

A simple and professional React component that demonstrates state management using the useState hook. The app displays a counter that increments each time the user clicks a button, updating the User instantly.
